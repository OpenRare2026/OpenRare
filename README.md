# search-agent

本仓库主代码已迁移至自包含子模块 **`module/pix_report/`**，可作为其他项目的 `module` 嵌入使用。

```bash
cd module/pix_report
pixi install
cp .env.example .env
pixi run api
```

详见 [module/pix_report/README.md](module/pix_report/README.md)。
