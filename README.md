# search-agent

本仓库主代码位于自包含子模块 **`modules/pixi_report/`**，可作为其他项目的 `modules` 嵌入使用。

```bash
cd modules/pixi_report
pixi install
cp .env.example .env
pixi run api
```

详见 [modules/pixi_report/README.md](modules/pixi_report/README.md)。
