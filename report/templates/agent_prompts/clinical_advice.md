请根据样本临床信息、HPO 表型和 Top 基因列表生成临床建议 JSON，字段：

- `immediate_recommendations`: 立即建议（字符串数组）
- `monitoring`: 动态监测（字符串数组）
- `communication_points`: 患者/家属沟通要点（字符串数组）
- `key_findings`: 关键发现提示（字符串数组）

不得编造宽表中的变异数值。
