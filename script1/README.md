# script1 旧版说明

`script1/` 是 Pixi 重构前的旧版运行目录，保留用于代码追溯和对比。

新部署和测试请优先使用：

```bash
cd ../pixi_ppi_score
pixi install
pixi run serve
```

旧版脚本仍可按原方式运行，但不再作为推荐入口：

```bash
./setup_env.sh
./run_api.sh
```

完整使用说明见仓库根目录 `README.md` 和 `pixi_ppi_score/README.md`。
