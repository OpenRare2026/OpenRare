[project]
name = "gateway"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []   # real deps are managed by pixi (conda) in [feature.orchestrator.dependencies]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/gateway"]
