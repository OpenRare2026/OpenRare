[project]
name = "module-a"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = []   # real deps are managed by pixi (conda) in [feature.module-a.dependencies]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/module_a"]
